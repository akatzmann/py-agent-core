## 1. Helper Implementation

- [x] 1.1 Implement `examples/utils.py` containing the `get_backend_from_args()` function.

## 2. Example Runner Refactoring

- [x] 2.1 Refactor `examples/rhetoric_speaker.py` to parse CLI args and dynamically select the backend.
- [x] 2.2 Refactor `examples/hierarchical_assistant.py` to parse CLI args and dynamically select the backend.
- [x] 2.3 Refactor `examples/search_watchdog.py` to parse CLI args and dynamically select the backend.
- [x] 2.4 Refactor `examples/structured_streaming.py` to parse CLI args and dynamically select the backend.

## 3. Verification

- [x] 3.1 Update integration tests in `tests/test_examples.py` to mock CLI arguments and verify correct backend resolution.
